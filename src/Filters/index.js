import React from 'react';
import Input from '../Input'

const Filters = () => {
    return (
        <React.Fragment>
            <p>Filters</p>
            <Input />
        </React.Fragment>
    );
}
 
export default Filters;